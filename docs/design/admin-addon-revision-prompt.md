# Claude Design Revision Prompt: Tencent CVM Admin Addon H5

你正在 Claude Design / Claude Artifacts 里继续修改当前已有的 OWP Tencent CVM 管理后台 H5 artifact。

不要重写整页，不要新建另一个版本，不要重新生成完整设计。请在现有页面基础上做最小修改，保留当前视觉风格、页面结构、样例数据和已有交互。

## 本次只修这些问题

1. 修复资源模板表格右侧被裁切的问题。
   - 在 1280 x 720 视口下，表格右侧不能超出页面。
   - 行内操作列必须完整可见。
   - 不能只露出「编辑」或只露出「复制」的一部分。
   - 页面不要出现横向滚动条来隐藏关键操作。

2. 资源模板每行操作必须完整显示:
   - 编辑
   - 复制
   - 校验
   - 停用

3. 行内操作必须是真实可点击控件。
   - 可以是小按钮、链接按钮或紧凑按钮组。
   - 不能只是静态文字。
   - 操作列可以更窄，但四个动作必须完整可见。

4. 点击任意一行的「停用」必须弹出确认弹窗。
   - 弹窗标题: 停用资源模板
   - 弹窗内容需要包含当前模板名称。
   - 弹窗内容需要说明: 停用后客户下单时将不可见。
   - 弹窗按钮: 取消、确认停用
   - 不要直接执行停用。

## 可以怎么压缩表格

如果当前表格太宽，请优先使用这些方式:

- 把「计费方式」和「系统盘类型/大小」保留在模板名称单元格第二行。
- 把「VPC / 子网」继续合并在网络单元格。
- 缩短状态和校验结果为徽章。
- 压缩按钮间距。
- 必要时降低单元格横向 padding。
- 必要时让镜像名称换行，但不要遮挡下一行。

不能删除这些关键信息:

- 模板名称
- 销售状态
- 地域 / 可用区
- 实例规格
- 镜像
- 网络
- 安全组
- 带宽
- 计费方式 / 系统盘
- 校验状态
- 编辑 / 复制 / 校验 / 停用

## 不要改成这些

- 不要生成设计说明页。
- 不要生成规格文档。
- 不要生成营销页或 hero。
- 不要改成客户区页面。
- 不要显示代码、变量名、模板占位符或开发者属性。
- 不要增加「桌面版」「移动版」「设计说明」「确认流程」这类交付说明标题。
- 不要展示真实 SecretKey、完整 API key、token 或生产密钥。

## 完成前自检

请在输出前检查:

- 1280 x 720 视口下资源模板表格没有右侧裁切。
- 每一行都能完整看到「编辑 / 复制 / 校验 / 停用」。
- 「复制」仍然存在。
- 计费方式和系统盘类型/大小仍然能直接看到。
- 顶部仍然能看到当前可售 2 个、模板总数 3 个。
- 点击「停用」会弹确认弹窗，不会直接执行。
- 页面仍然是 WHMCS 后台 addon 页面，不是设计交付长画布。
